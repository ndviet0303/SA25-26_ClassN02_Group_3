# Notification Service Configuration (Docker/Production overrides)

spring:
  data:
    mongodb:
      uri: mongodb://mongodb:27017/notificationdb
    redis:
      host: redis
  rabbitmq:
    host: rabbitmq

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
