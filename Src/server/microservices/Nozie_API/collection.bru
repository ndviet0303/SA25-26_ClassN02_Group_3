meta {
  name: Nozie
}

auth {
  mode: none
}

vars:pre-request {
  baseUrl: http://localhost:8080
  accessToken: 
  refreshToken: 
  userId: 
  adminToken: 
}

docs {
  Complete API collection for Nozie Identity Service with Authentication, Authorization, RBAC, and Admin features
}
