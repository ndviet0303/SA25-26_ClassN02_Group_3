meta {
  name: 8.2 Login Admin
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "admin",
    "password": "admin123"
  }
}

script:post-response {
  var jsonData = res.getBody();
  if (jsonData.success && jsonData.data) {
      bru.setVar('adminToken', jsonData.data.accessToken);
  }
}

settings {
  encodeUrl: true
}
