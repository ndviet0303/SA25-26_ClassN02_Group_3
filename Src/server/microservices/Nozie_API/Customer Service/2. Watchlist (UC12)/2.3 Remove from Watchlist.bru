meta {
  name: 2.3 Remove from Watchlist
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/customers/{{customerId}}/watchlist/{{movieId}}
  body: none
  auth: inherit
}

vars:pre-request {
  customerId: 1
  movieId: movie-123
}

docs {
  Remove a movie from customer's watchlist (UC12).
}

settings {
  encodeUrl: true
}
