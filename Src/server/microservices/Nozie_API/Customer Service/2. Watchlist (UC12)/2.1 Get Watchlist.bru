meta {
  name: 2.1 Get Watchlist
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/customers/{{customerId}}/watchlist
  body: none
  auth: inherit
}

vars:pre-request {
  customerId: 7
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Get all movies in customer's watchlist (UC12).
}
