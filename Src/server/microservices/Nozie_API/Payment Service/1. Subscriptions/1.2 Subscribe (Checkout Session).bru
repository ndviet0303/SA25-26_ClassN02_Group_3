meta {
  name: 1.2 Subscribe (Checkout Session)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/subscriptions/subscribe
  body: json
  auth: inherit
}

body:json {
  {
    "userId": 22,
    "planType": "STUDENT_MONTHLY"
  }
}
